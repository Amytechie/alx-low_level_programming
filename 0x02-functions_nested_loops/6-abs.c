#include "main.h"

/**
 * _abs - this prints the absolute value
 *
 * @n: this is the integer
 *
 * Return: this is the return value
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
